# Follow these steps to run the application:-
1. Run `npm install`
2. Run `npm start` to start the application, application will run at htttp://localhost:5000
3. Run `npm test` to test the application.